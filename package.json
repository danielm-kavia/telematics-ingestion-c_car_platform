{
  "name": "@connected-car/telematics-ingestion",
  "version": "0.1.0",
  "private": true,
  "description": "Telematics ingestion service: HTTP + Kafka consumer to TimescaleDB.",
  "main": "src/server.js",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "lint": "node -e \"console.log('lint placeholder (Phase 3)')\"",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:integration": "VEHICLE_STATE_TEST_PORT=3101 TELEMATICS_INGESTION_TEST_PORT=3102 VEHICLE_STATE_BASE_URL=http://127.0.0.1:3101 TELEMATICS_INGESTION_BASE_URL=http://127.0.0.1:3102 TIMESCALE_ENABLED=false KAFKA_ENABLED=false jest --config jest.integration.config.js --runInBand",
    "test:integration:coverage": "VEHICLE_STATE_TEST_PORT=3101 TELEMATICS_INGESTION_TEST_PORT=3102 VEHICLE_STATE_BASE_URL=http://127.0.0.1:3101 TELEMATICS_INGESTION_BASE_URL=http://127.0.0.1:3102 TIMESCALE_ENABLED=false KAFKA_ENABLED=false jest --config jest.integration.coverage.config.js --runInBand"
  },
  "dependencies": {
    "@connected-car/shared": "file:../shared-c_car_platform",
    "express": "4.21.2",
    "kafkajs": "^2.2.4",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "jest": "^30.0.0",
    "jest-junit": "^16.0.0",
    "supertest": "^7.2.2"
  }
}
