# telematics-ingestion-c_car_platform

Telematics ingestion service container (placeholder).

## Local development

### Environment
Copy and edit:
- `.env.example` -> `.env`

### Commands (planned)
If/when a Node.js implementation is added:

```bash
npm install
npm run dev
npm run lint
npm run format
npm test
```
